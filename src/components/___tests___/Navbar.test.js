import React from 'react';
import { render } from '@testing-library/react';
import Navbar from '../Navbar';

test('Navbar renders without errors', () => {
  render(<Navbar />);
  // Add assertions here to check if Navbar is rendered properly
});
