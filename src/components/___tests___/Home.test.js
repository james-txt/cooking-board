import React from 'react';
import { render } from '@testing-library/react';
import Home from '../Home';

test('Home component renders without errors', () => {
  render(<Home />);
  // Add assertions here to check if Home is rendered properly
});
