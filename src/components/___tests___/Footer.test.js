import React from 'react';
import { render } from '@testing-library/react';
import Footer from '../Footer';

test('Footer renders without errors', () => {
  render(<Footer />);
  // Add assertions here to check if Footer is rendered properly
});
